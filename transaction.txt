SELECT * FROM `farmer`;
START TRANSACTION;
UPDATE farmer SET age = 20 WHERE farmer_id = 1;
SELECT SLEEP(20);
ROLLBACK;



SET TRANSACTION ISOLATION LEVEL READ UNCOMMITTED;
START TRANSACTION;
SELECT *from farmer;
COMMIT;